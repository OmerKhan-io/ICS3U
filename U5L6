import math
print("  N|   SQR|   Cubes| Roots")
print("---+------+--------+-------")
squared = 0
cubes = 0
for i in range (10,200,15):
  squared = int(math.pow(i,2))
  cubes = int(math.pow(i,3))
  roots = math.sqrt(i)
  
  print("%3d| %5d| %7d| %5.2f|" % (i,squared, cubes, roots))
